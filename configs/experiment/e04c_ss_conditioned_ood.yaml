# Phase 6.3 ablation arm c -- sea-state conditioning -- one-hot SS as an input feature (closed-form vehicles, OOD regimes).
#
# AN UPPER BOUND, NOT A DEPLOYABLE RESULT: at deployment sea state is estimated online from the
# same record the forecaster consumes (IMPLEMENTATION_PLAN 6.3; P6-D4 item 5).
#
# `ar_p20` is ABSENT from this file and present in the `_ood` and `_ar_id` twins, and that is
# not an oversight. On `unseen_seastate` the SS6 indicator is constant zero in training, the
# normal equations are singular, and `_solve_centred_whitened` refuses them -- which is the
# arm's actual finding, recorded in P6-D8 defect 2. Including it here would abort the run
# instead of reporting the row as `not fittable`.
#
# `dlinear_ols` is retained despite being structurally blind to the indicator (DLinear slices
# `x[:, :, :C_out]`, P6-D8 defect 1). Its contrast is exactly zero as an architectural
# artefact, and it ships flagged rather than dropped -- a zero that is not a null result is
# precisely the number a reader would otherwise misread.
#
# The `train` block is byte-identical to `configs/experiment/e02_deep.yaml`: epochs 60,
# batch 1024, lr 2e-3, patience 15, bf16. One budget, one schedule, one stopping rule, so the
# arms differ in the DATA definition and in nothing else. Changing it would make this a
# comparison of two training regimes wearing an ablation's name.
#
# The reference arm is NOT re-run. `results/e02/` already fitted `tcn` (3 seeds) and
# `dlinear_ols` on every regime under this exact train block and `configs/data/default.yaml`;
# its committed rows are read, never regenerated (docs/IMPLEMENTATION_PLAN.md Phase 6
# carry-forward item 7). The one exception is the lookback ablation, where the reference is
# re-SCORED from e02's checkpoints on the window-matched origin set (P6-D4 item 1) -- a
# forward pass, not a refit.
#
# `persistence` must be first: it is the skill-score denominator and
# `dmf.eval.runner.evaluate_models` requires it.
#
# The closed-form half of arm c. `tcn` is absent deliberately: it is budgeted for `id` and
# `unseen_seastate` only, and the deterministic vehicles here cost minutes, so this file is
# what makes the linear reading of every ablation four-regime instead of two.
name: e04c_ss_conditioned_ood
data: ../data/ss_conditioned.yaml
models:
  - ../model/persistence.yaml
  - ../model/window_mean.yaml
  - ../model/damped_persistence.yaml
  - ../model/ar_p20.yaml
  - ../model/dlinear_ols.yaml

train:
  epochs: 60
  batch_size: 1024
  lr: 2.0e-3
  weight_decay: 1.0e-4
  warmup_frac: 0.05
  grad_clip: 1.0
  patience: 15
  amp_dtype: bf16
  num_workers: 16

seeds: [0, 1, 2]
regimes: [unseen_heading, unseen_vessel]
